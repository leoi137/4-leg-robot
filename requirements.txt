# Known-good pins for the spyder project (Python 3.14).
# Install into the repo venv:  ./mujoco-env/bin/pip install -r requirements.txt
gymnasium[mujoco]==1.3.0
mujoco==3.9.0
glfw==2.10.0
numpy==2.4.6
imageio
# Note: CAD mesh authoring (build123d) lives in a SEPARATE Python 3.13 venv —
# see requirements-cad.txt. build123d has no 3.14 wheel, so it is not installed here.
